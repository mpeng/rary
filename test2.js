origArray = ["B", "C", "B", "B", "B", "A", "D"]; 
filterArray = ["D"];
map = {};

for ( i = 0; i < origArray.length; i++ ) {
	console.log(origArray[i]);
	if ( map.has( origArray[i]) ) {
        
    }

}